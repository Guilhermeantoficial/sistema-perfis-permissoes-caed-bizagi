apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: caed-perfis-permissoes-web
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: caed-perfis-permissoes
      component: web
